{
	"name": "@flight-booking/shared",
	"version": "1.0.0",
	"description": "Shared types, constants, and utilities for flight booking application",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"scripts": {
		"build": "tsc",
		"dev": "tsc --watch",
		"clean": "rm -rf dist",
		"lint": "echo 'No linting configured yet'",
		"test": "echo 'No tests configured yet'"
	},
	"devDependencies": {
		"typescript": "^5.7.3"
	},
	"files": [
		"dist"
	],
	"publishConfig": {
		"access": "restricted"
	}
}
